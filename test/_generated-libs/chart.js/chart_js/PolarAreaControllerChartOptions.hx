package chart_js;

typedef PolarAreaControllerChartOptions = {
	/**
		Starting angle to draw arcs for the first item in a dataset. In degrees, 0 is at top.
	**/
	var startAngle : Float;
	var animation : DoughnutAnimationOptions;
};